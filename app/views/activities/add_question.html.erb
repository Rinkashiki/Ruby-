<%= stylesheet_link_tag 'users' %>   
  <div class="container form_padding">
    <br>
    <div class="form-group">
        <% flash.each do |type, msg| %>
          <div class="alert alert-danger alert-dismissible d-inline-block" role="alert">
            <%= msg %>
          </div> 
        <% end %>
        <% flash.delete(:alert) %>
        <h1>Añadir Pregunta</h1>
        <div class="m-4">
        <%= link_to 'Trivia', add_question_path(type: 'Trivia'), class: 'btn btn-primary' %>
        <%= link_to 'Video Trivia', add_question_path(type: 'Video Trivia'), class: 'btn btn-primary' %>
        <%= link_to 'Video Test', add_question_path(type: 'Video Test'), class: 'btn btn-primary' %>
        <%= link_to 'Pregunta Abierta', add_question_path(type: 'Pregunta Abierta'), class: 'btn btn-primary' %>
        </div>
        <div id="questionForm">
        <% if @type.nil? || @type == 'Trivia' %>
          <%= render 'trivia_form' %>
        <% end %>
        <% if @type == 'Video Trivia' %>
          <%= render 'video_trivia_form' %>
          <% end %>
        <% if @type == 'Video Test' %>
          <%= render 'video_test_form' %>
        <% end %>
        <% if @type == 'Pregunta Abierta' %>
          <%= render 'open_question_form' %>
        <% end %>
        </div>
    </div>
  </div>

